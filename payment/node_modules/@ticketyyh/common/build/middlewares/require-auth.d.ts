/**
 Middle requireAuth
    1. Middle that reject the request if the user is not logged in
 */
import { Request, Response, NextFunction } from 'express';
export declare const requireAuth: (req: Request, res: Response, next: NextFunction) => void;
